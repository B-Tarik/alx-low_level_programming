#include <stdio.h>
/**
 * main -  program that prints its name
 * @argc: count
 * @argv: array of pointers
 * Return: 0
 **/
int main(int argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
